(function(window) {
	jsPages.Manager.register(
			"SPLASH",
			new jsPages.Page()
					.setHtmlPath("Splash")
	);

	jsPages.Manager.register(
			"GAME_BOARD",
			new jsPages.Page()
					.setHtmlPath("GameBoard")
	);
} (window));
